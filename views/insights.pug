table
    thead
        tr
            th Metric
            th Value
            th Description
            th Id
    tbody
        each metric in metrics
            tr
                td #{metric.name}
                td #{metric.value}
                td #{metric.description}
                td #{metric.id}
